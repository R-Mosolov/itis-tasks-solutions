// Вычисление суммы
// 1 + (x / (2 + (x / (3 + (x / 4 + (...))))))

function calculateSum(x, nestingLevel) {
    for (let i = 0; i <= nestingLevel; i++) {

    }
}

alert(calculateSum(2));

// Можно решить с помощью рекурсии
// Решить с помощью цикла